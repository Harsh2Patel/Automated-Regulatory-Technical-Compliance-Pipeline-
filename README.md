This project is an automated backend data pipeline that tracks engineering and technical standard updates by fetching real-time data from the Federal Register API. Built with Python and transitioned into the Django framework, the application utilizes robust UPSERT logic to ensure data integrity by preventing duplicate entries as it stores new records in an SQLite database. Additionally, it features a decoupled reporting module that programmatically processes raw database records into clean, human-readable markdown summaries.
